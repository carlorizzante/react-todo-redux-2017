const expect = require("expect");

// Verify that tests are allowed to run,
// Not much else to see in here.

describe("App", () => {
  it("should properly run generic tests", () => {
    expect(1 + 2).toBe(3);
  });
});
